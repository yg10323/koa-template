const { sequelize, Op } = require('@src/db')
const initModels = require('@src/model/init-models')
const logger = require('@src/utils/log4');

const Models = initModels(sequelize)

<% include('./functions') %>
class <%= firstToUpperCase(config.tableName) %>Service {
  // 示例
  async expService (...args) {
    try {
      // your service logic, such as:
      return await Models['<%= config.tableName %>'].findAll({ raw: true });
    } catch (error) {
      logger.error('<%= firstToUpperCase(config.tableName) %>Service_exaService_', error)
    }
  }
}

// 导出的为<%= firstToUpperCase(config.tableName) %>Service的实例
// 使用见生成的对应的.controller文件
module.exports = new <%= firstToUpperCase(config.tableName) %>Service()